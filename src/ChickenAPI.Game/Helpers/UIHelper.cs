namespace ChickenAPI.Game.Helpers
{
    public class UIHelper
    {
        // GenerateMessage (string + enum MessageType)
        // GenerateDialog (question + Accept Packet + Refuse Packet)
        // GenerateChatMessage
    }
}